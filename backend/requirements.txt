# ── Core API ──────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.5
pydantic-settings==2.9.1
python-multipart==0.0.20
python-dotenv==1.1.0
starlette==0.49.1

# ── Supabase ──────────────────────────────────────────────────
supabase==2.15.3
httpx==0.28.1

# ── gRPC (required by supabase / qdrant) ──────────────────────
grpcio==1.71.0
grpcio-tools==1.71.0

# ── Utilities ─────────────────────────────────────────────────
requests==2.32.3
numpy==2.2.6
python-dateutil==2.9.0.post0
networkx==3.4.2

# ── AI / Vector / Graph ───────────────────────────────────────
cohere==5.15.0
qdrant-client==1.14.2
neo4j==5.28.1

# ── PDF Export ────────────────────────────────────────────────
reportlab==4.4.1

# NOTE: jina package removed — the app uses the Jina REST API
# directly via HTTP (JINA_API_KEY env var). The jina pip package
# (600MB framework) is not imported anywhere in the codebase.
